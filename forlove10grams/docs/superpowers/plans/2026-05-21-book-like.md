# Book Like 功能 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓讀者在書末主動 Like 一本書；Creator 在 dashboard 書本卡片看到匿名 like 數（N=0 時不顯示）。

**Architecture:** 新增 `BookLike` model 記錄按讚關係；`getLikeCountsByBook` helper 共用於 SSR 和 API；Like 按鈕出現在閱讀頁最末，toggle 支援取消並做 optimistic update。Creator 端 API 只回傳 count，不揭露 userId。

**Tech Stack:** Next.js 16 App Router, Mongoose 8, TypeScript strict, Tailwind CSS

---

## 檔案結構

| 路徑 | 動作 | 說明 |
|------|------|------|
| `lib/models/book-like.ts` | **新增** | BookLike Mongoose model |
| `lib/queries/book-like-counts.ts` | **新增** | Aggregation helper：bookId → likeCount |
| `app/api/books/[bookId]/like/route.ts` | **新增** | POST toggle like API |
| `app/api/books/route.ts` | **修改** | GET 書單加上 likeCount 欄位 |
| `components/dashboard-books-client.tsx` | **修改** | DashboardBook 型別 + BookCard 顯示 ♡ N |
| `app/dashboard/page.tsx` | **修改** | SSR 計算 likeCount 並傳入卡片 |
| `app/read/[bookId]/page.tsx` | **修改** | SSR 查詢 hasLiked 並傳入 client |
| `components/read-page-client.tsx` | **修改** | 書末 LikeButton 元件 |

---

## Task 1: BookLike Model

**Files:**
- Create: `lib/models/book-like.ts`

- [ ] **Step 1: 建立 model 檔案**

建立 `forlove10grams/lib/models/book-like.ts`：

```ts
import mongoose, { Schema, type Document, type Model, type Types } from 'mongoose'

export interface IBookLike extends Document {
  bookId: Types.ObjectId
  userId: Types.ObjectId
  likedAt: Date
}

const BookLikeSchema = new Schema<IBookLike>({
  bookId: { type: Schema.Types.ObjectId, ref: 'Book', required: true },
  userId: { type: Schema.Types.ObjectId, ref: 'User', required: true },
  likedAt: { type: Date, default: () => new Date() },
})

BookLikeSchema.index({ bookId: 1, userId: 1 }, { unique: true })

const BookLike: Model<IBookLike> =
  mongoose.models.BookLike ?? mongoose.model<IBookLike>('BookLike', BookLikeSchema)

export default BookLike
```

- [ ] **Step 2: Lint 驗證**

在 `forlove10grams/` 目錄執行：

```bash
npm run lint
```

Expected: 無錯誤輸出，exit code 0。

- [ ] **Step 3: Commit**

```bash
git add forlove10grams/lib/models/book-like.ts
git commit -m "feat: add BookLike mongoose model"
```

---

## Task 2: getLikeCountsByBook Helper

**Files:**
- Create: `lib/queries/book-like-counts.ts`

- [ ] **Step 1: 建立 queries 目錄與 helper 檔案**

建立 `forlove10grams/lib/queries/book-like-counts.ts`：

```ts
import type { Types } from 'mongoose'
import BookLike from '@/lib/models/book-like'

export async function getLikeCountsByBook(
  bookIds: Types.ObjectId[]
): Promise<Map<string, number>> {
  if (bookIds.length === 0) return new Map()

  const results = await BookLike.aggregate<{ _id: Types.ObjectId; count: number }>([
    { $match: { bookId: { $in: bookIds } } },
    { $group: { _id: '$bookId', count: { $sum: 1 } } },
  ])

  return new Map(results.map((r) => [r._id.toString(), r.count]))
}
```

- [ ] **Step 2: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。

- [ ] **Step 3: Commit**

```bash
git add forlove10grams/lib/queries/book-like-counts.ts
git commit -m "feat: add getLikeCountsByBook aggregation helper"
```

---

## Task 3: Like Toggle API

**Files:**
- Create: `app/api/books/[bookId]/like/route.ts`

- [ ] **Step 1: 建立 API route**

建立 `forlove10grams/app/api/books/[bookId]/like/route.ts`：

```ts
import type { NextRequest } from 'next/server'
import { auth } from '@/auth'
import { dbConnect } from '@/lib/mongoose'
import Book from '@/lib/models/book'
import BookLike from '@/lib/models/book-like'
import { canEditBook, isBookReader } from '@/lib/access'

export async function POST(
  _req: NextRequest,
  ctx: RouteContext<'/api/books/[bookId]/like'>
) {
  const session = await auth()
  if (!session?.user?.id) {
    return Response.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const { bookId } = await ctx.params
  const userId = session.user.id

  await dbConnect()
  const book = await Book.findById(bookId)
  if (!book) {
    return Response.json({ error: 'Not found' }, { status: 404 })
  }

  const canAccess =
    canEditBook(userId, book) ||
    book.published ||
    (await isBookReader(userId, bookId))
  if (!canAccess) {
    return Response.json({ error: 'Forbidden' }, { status: 403 })
  }

  const existing = await BookLike.findOne({ bookId: book._id, userId })
  if (existing) {
    await existing.deleteOne()
    return Response.json({ liked: false })
  }

  await BookLike.create({ bookId: book._id, userId })
  return Response.json({ liked: true })
}
```

- [ ] **Step 2: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。

- [ ] **Step 3: Commit**

```bash
git add forlove10grams/app/api/books/[bookId]/like/route.ts
git commit -m "feat: add POST /api/books/[bookId]/like toggle endpoint"
```

---

## Task 4: DashboardBook 型別 + 擴充 GET /api/books

**Files:**
- Modify: `components/dashboard-books-client.tsx` (型別定義，約第 10-17 行)
- Modify: `app/api/books/route.ts` (GET handler，約第 8-49 行)

- [ ] **Step 1: 更新 DashboardBook 型別**

在 `forlove10grams/components/dashboard-books-client.tsx` 的 `DashboardBook` 型別加入 `likeCount`：

```ts
export type DashboardBook = {
  _id: string
  title: string
  description: string | null
  coverImage: string | null
  published: boolean
  tags: string[]
  likeCount: number   // 新增
}
```

- [ ] **Step 2: 擴充 GET /api/books**

在 `forlove10grams/app/api/books/route.ts` 頂部加入 import：

```ts
import mongoose from 'mongoose'
import BookLike from '@/lib/models/book-like'
import { getLikeCountsByBook } from '@/lib/queries/book-like-counts'
```

（`mongoose` import 已存在，只需加 `BookLike` 和 `getLikeCountsByBook`。）

將 `GET` handler 中書單查詢後的回傳部分改為：

```ts
  const books = await Book.find(query)
    .sort({ _id: -1 })
    .limit(limit)
    .select('_id title description coverImage published tags')
    .lean()

  const bookObjectIds = books.map((b) => b._id as mongoose.Types.ObjectId)
  const likeCounts = await getLikeCountsByBook(bookObjectIds)

  return Response.json(
    books.map((b) => ({
      _id: b._id.toString(),
      title: b.title,
      description: b.description ?? null,
      coverImage: b.coverImage ?? null,
      published: b.published ?? false,
      tags: (b as { tags?: string[] }).tags ?? [],
      likeCount: likeCounts.get(b._id.toString()) ?? 0,
    }))
  )
```

- [ ] **Step 3: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。如果 lint 報告 `likeCount` 不在某型別中，確認 `DashboardBook` 型別已更新。

- [ ] **Step 4: Commit**

```bash
git add forlove10grams/components/dashboard-books-client.tsx forlove10grams/app/api/books/route.ts
git commit -m "feat: add likeCount to DashboardBook type and GET /api/books response"
```

---

## Task 5: Dashboard SSR likeCount

**Files:**
- Modify: `app/dashboard/page.tsx`

- [ ] **Step 1: 加入 import**

在 `forlove10grams/app/dashboard/page.tsx` 頂部加入：

```ts
import BookLike from '@/lib/models/book-like'
import { getLikeCountsByBook } from '@/lib/queries/book-like-counts'
```

- [ ] **Step 2: 更新 toBook 函式簽名**

將現有的 `toBook` 函式改為接收第二個參數 `likeCount`：

```ts
function toBook(
  b: { _id: mongoose.Types.ObjectId; title: string; description?: string; coverImage?: string; published?: boolean; tags?: string[] },
  likeCount = 0
): DashboardBook {
  return {
    _id: b._id.toString(),
    title: b.title,
    description: b.description ?? null,
    coverImage: b.coverImage ?? null,
    published: b.published ?? false,
    tags: b.tags ?? [],
    likeCount,
  }
}
```

- [ ] **Step 3: 在 DashboardPage 計算並傳遞 likeCount**

在 `DashboardPage` 的 `Promise.all` 之後，加入 likeCount 查詢（僅 admin 需要）：

```ts
  const [ownerBooksRaw, editorBooksRaw, progressAgg] = await Promise.all([
    isAdmin
      ? Book.find({ createdBy: uid }).sort({ _id: -1 }).limit(INITIAL_LIMIT).lean()
      : [],
    Book.find({ editorId: uid }).sort({ _id: -1 }).lean(),
    ReadProgress.aggregate<{ _id: mongoose.Types.ObjectId; count: number }>([
      { $match: { userId: uid } },
      { $group: { _id: '$bookId', count: { $sum: 1 } } },
    ]),
  ])

  // 新增：計算 ownerBooks 的 likeCount
  const ownerLikeCounts = isAdmin
    ? await getLikeCountsByBook(ownerBooksRaw.map((b) => b._id as mongoose.Types.ObjectId))
    : new Map<string, number>()
```

再將 `ownerBooks` 的映射改為：

```ts
  const ownerBooks = ownerBooksRaw.map((b) =>
    toBook(b, ownerLikeCounts.get(b._id.toString()) ?? 0)
  )
```

（其餘使用 `toBook` 的地方—`editorBooksRaw` 和 `readerBooksRaw`—維持 `toBook(b)` 不變，likeCount 預設為 0，不影響 UI。）

- [ ] **Step 4: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。

- [ ] **Step 5: Commit**

```bash
git add forlove10grams/app/dashboard/page.tsx
git commit -m "feat: compute likeCount in dashboard SSR"
```

---

## Task 6: BookCard ♡ N 顯示

**Files:**
- Modify: `components/dashboard-books-client.tsx` (BookCard 元件，約第 22-105 行)

- [ ] **Step 1: 在 BookCard 加入 likeCount 顯示**

在 `forlove10grams/components/dashboard-books-client.tsx` 的 `BookCard` 元件內，找到顯示狀態 badge 的區塊：

```tsx
<span
  className={`text-xs px-2 py-0.5 rounded-full ${
    book.published
      ? 'bg-emerald-50 text-emerald-700'
      : 'bg-[#2C1810]/5 text-[#2C1810]/40'
  }`}
>
  {book.published ? '已分享' : '草稿'}
</span>
```

在此 `<span>` 之前插入 like 數顯示（likeCount > 0 時才顯示）：

```tsx
{book.likeCount > 0 && (
  <span className="text-xs text-[#2C1810]/40">
    ♡ {book.likeCount}
  </span>
)}
```

完整的 `shrink-0 flex items-center gap-2` div 變成：

```tsx
<div className="shrink-0 flex items-center gap-2">
  {book.likeCount > 0 && (
    <span className="text-xs text-[#2C1810]/40">
      ♡ {book.likeCount}
    </span>
  )}
  <span
    className={`text-xs px-2 py-0.5 rounded-full ${
      book.published
        ? 'bg-emerald-50 text-emerald-700'
        : 'bg-[#2C1810]/5 text-[#2C1810]/40'
    }`}
  >
    {book.published ? '已分享' : '草稿'}
  </span>
  <button
    type="button"
    onClick={() => setShowTagModal(true)}
    className="text-xs text-[#2C1810]/40 hover:text-[#2C1810]/70 transition-colors px-1"
    title="管理標籤"
  >
    ＋標籤
  </button>
  <span className="text-[#2C1810]/30">
    <PencilIcon />
  </span>
</div>
```

- [ ] **Step 2: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。

- [ ] **Step 3: Commit**

```bash
git add forlove10grams/components/dashboard-books-client.tsx
git commit -m "feat: show like count on dashboard book card"
```

---

## Task 7: 閱讀頁 Like 按鈕

**Files:**
- Modify: `app/read/[bookId]/page.tsx`
- Modify: `components/read-page-client.tsx`

- [ ] **Step 1: 更新 ReadPageClient Props 型別**

在 `forlove10grams/components/read-page-client.tsx` 的 `Props` 型別加入 `hasLiked`：

```ts
type Props = {
  bookId: string
  bookTitle: string
  initialPages: ReadPageData[]
  totalCount: number
  viewerNickname: string | null
  viewerMyNickname: string | null
  hasLiked: boolean   // 新增
}
```

並在 `ReadPageClient` 函式解構中加入：

```ts
export function ReadPageClient({ bookId, bookTitle, initialPages, totalCount, viewerNickname, viewerMyNickname, hasLiked }: Props) {
```

- [ ] **Step 2: 建立 LikeButton 元件（同檔案內）**

在 `read-page-client.tsx` 的 `ReadPageClient` 函式定義**之前**，加入 `LikeButton` 元件：

```tsx
function LikeButton({ bookId, initialHasLiked }: { bookId: string; initialHasLiked: boolean }) {
  const [liked, setLiked] = useState(initialHasLiked)
  const [pending, setPending] = useState(false)

  const toggle = async () => {
    if (pending) return
    const next = !liked
    setLiked(next)
    setPending(true)
    try {
      const res = await fetch(`/api/books/${bookId}/like`, { method: 'POST' })
      if (!res.ok) throw new Error('failed')
      const data: { liked: boolean } = await res.json()
      setLiked(data.liked)
    } catch {
      setLiked(!next)
    } finally {
      setPending(false)
    }
  }

  return (
    <button
      onClick={toggle}
      disabled={pending}
      className="flex flex-col items-center gap-3 text-[#2C1810]/50 hover:text-[#A0826D] transition-colors disabled:opacity-40"
    >
      <span className="text-4xl leading-none">{liked ? '❤️' : '🤍'}</span>
      <span className="text-xs tracking-wide">
        {liked ? '謝謝你喜歡這本書' : '謝謝你讀完了'}
      </span>
    </button>
  )
}
```

`useState` 已透過 `'use client'` 指令在檔案頂部可用（`import { useMemo, useRef, useCallback } from 'react'`），加入 `useState` 到現有的 import：

```ts
import { useState, useMemo, useRef, useCallback } from 'react'
```

- [ ] **Step 3: 在書末渲染 LikeButton**

在 `ReadPageClient` 的 return 中，找到目前的 sentinel div：

```tsx
{hasMore && (
  <div ref={sentinelRef} className="h-20" aria-hidden />
)}
```

在其後加入書末 Like 按鈕（只有全部頁面載入完成且有內容時顯示）：

```tsx
{hasMore && (
  <div ref={sentinelRef} className="h-20" aria-hidden />
)}

{!hasMore && pages.length > 0 && (
  <div className="mt-16 mb-12 flex justify-center">
    <LikeButton bookId={bookId} initialHasLiked={hasLiked} />
  </div>
)}
```

- [ ] **Step 4: 更新閱讀頁 SSR（`app/read/[bookId]/page.tsx`）**

加入 import：

```ts
import BookLike from '@/lib/models/book-like'
```

在 `dbConnect()` 之後，`book` 查詢之後，加入 hasLiked 查詢（緊接在 `canAccess` 判斷通過後）：

在 `return ( <ReadPageClient ...` 之前加：

```ts
  const hasLiked = !!(await BookLike.exists({ bookId: book._id, userId }))
```

並在 `<ReadPageClient />` 加入 prop：

```tsx
  return (
    <ReadPageClient
      bookId={bookId}
      bookTitle={book.title}
      initialPages={initialPages}
      totalCount={totalCount}
      viewerNickname={viewerNickname}
      viewerMyNickname={viewerMyNickname}
      hasLiked={hasLiked}
    />
  )
```

- [ ] **Step 5: Lint 驗證**

```bash
npm run lint
```

Expected: 無錯誤，exit code 0。

- [ ] **Step 6: Commit**

```bash
git add forlove10grams/app/read/[bookId]/page.tsx forlove10grams/components/read-page-client.tsx
git commit -m "feat: add like button at end of reading page"
```

---

## Task 8: 手動驗收測試

- [ ] **Step 1: 啟動 dev server**

```bash
npm run dev
```

- [ ] **Step 2: 驗收讀者 Like 功能**

1. 以讀者身份（非 admin）登入，開啟一本已分享的書
2. 捲動到書末，確認 Like 按鈕出現（`🤍 謝謝你讀完了`）
3. 點擊按鈕，確認即時變為 `❤️ 謝謝你喜歡這本書`（optimistic update）
4. 重新整理頁面，確認 like 狀態持久化（仍顯示 `❤️`）
5. 再次點擊，確認 toggle 取消（回到 `🤍`）

- [ ] **Step 3: 驗收 Creator Dashboard**

1. 以 admin 身份登入，查看 dashboard
2. 剛才讀者 like 的那本書卡片應顯示 `♡ 1`
3. 未被 like 的書卡片不顯示任何 like 資訊

- [ ] **Step 4: 邊界條件確認**

- 開啟一本沒有頁面的書（若存在），確認書末不出現 Like 按鈕
- 以同一讀者重複開啟書本，確認 like 狀態正確讀回

- [ ] **Step 5: 最終 lint 確認**

```bash
npm run lint
```

Expected: exit code 0。
