寫入權限被拒絕，改為直接輸出報告內容：

# 效能審核報告 2026-07-03-073221

## 判斷結果
WARN

## 涉及的高風險改動
- `forlove10grams/app/dashboard/page.tsx`：Server Component 內容有實質變更（新增 `isAdmin ? <QuickCaptureBar /> : null` 並傳入 `DashboardShell`），符合「page.tsx 有實質內容變更」規則。
- `forlove10grams/components/quick-capture-bar.tsx`：新檔案，帶有 `'use client'`，在 dashboard 首屏（`-mt-4` 緊接搜尋框下方）常駐渲染，內含 3 個 `fetch` 呼叫按鈕、ripple 效果與狀態管理。屬於「新增 use client directive」情況。
- `forlove10grams/components/dashboard-books-client.tsx`：既有大型 Client Component（`DashboardShell`）新增 `quickCapture` prop 並在首屏渲染區塊插入，影響範圍是核心 dashboard 頁面。

## 對應的效能 log
找不到對應記錄。`.perf-log/` 中僅有前一筆 `audit-2026-07-03-072143.md`，但該筆審核的是後端 `app/api/books/quick/route.ts`（API route），不涵蓋這次的前端 dashboard 頁面渲染與新 client component。

## 建議
建議跑一次 dashboard 頁面的效能檢查（首屏渲染 / TTI，尤其確認新增的 client component 對 dashboard 這種資料量較大的頁面是否有明顯 hydration 成本），再補一筆 perf-log。

建議 commit message：

```
feat(dashboard): add quick capture bar to dashboard shell

[perf-needed: /dashboard]
```

## 模糊地帶（需人工判斷）
- `QuickCaptureBar` 只有 `isAdmin` 才會渲染，且元件本身邏輯輕量（3 顆按鈕 + fetch），實際效能影響可能很小，開發者可自行判斷是否值得為此單獨測。
- `DashboardShell` 已是既有 Client Component，此次只是新增一個 optional prop 與一段條件渲染，非新增 client boundary，風險相對低——但因為 dashboard 是高流量頁面，建議仍列入觀察名單。

要我改用你核准的方式把這份報告寫進 `.perf-log/audit-2026-07-03-073221.md` 嗎？
