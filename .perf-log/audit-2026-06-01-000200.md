現在我有足夠資訊來產出報告了。

```markdown
# 效能審核報告 2026-06-01-000200

## 判斷結果
WARN

## 涉及的高風險改動
- `app/books/[bookId]/edit/page.tsx`：layout 有實質變更
  - `h-screen` → `min-h-dvh md:h-dvh overflow-x-hidden`
  - 這會影響行動裝置的初始 viewport 高度計算，可能影響 CLS

## 對應的效能 log
找不到對應記錄（`.perf-log/` 目前為空）

## 建議
建議在行動裝置上跑一次 `/books/[bookId]/edit` 的 Lighthouse 或 WebPageTest，
特別確認 CLS 是否因 `min-h-dvh` 切換而上升。

建議 commit message：

```
fix(book-editor): 修正行動裝置版面排版與觸控目標大小

[perf-needed: /books/[bookId]/edit]
```

## 模糊地帶（需人工判斷）

1. **`useEffect` + `window.matchMedia`（book-editor-client.tsx）**
   新增 isMobile 狀態，用 matchMedia 監聽視窗寬度。Client-side only，但會造成 hydration 時 `isMobile` 為 false 再切換，已加 `suppressHydrationWarning`。
   → 確認 MDEditor 在行動裝置首次渲染時的 preview mode 切換是否造成 layout shift

2. **MDEditor `preview` 模式切換**（`'live'` → `'edit'` on mobile）
   改變了 MDEditor 的渲染模式，mobile 省略 preview pane，可能影響 LCP（少渲染一個 pane 通常是改善，但值得確認無副作用）
```
