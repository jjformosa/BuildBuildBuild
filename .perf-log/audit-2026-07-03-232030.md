Permission to write the file wasn't granted. Since the instructions say the report will be saved by the caller, here's the output directly:

```markdown
# 效能審核報告 2026-07-03-232030

## 判斷結果
PASS

## 涉及的高風險改動
無。本次 staged 檔案僅為兩份 `docs/superpowers/plans/*.md` 規劃文件，不含任何 `app/`、元件、資料獲取或第三方套件變更。

## 對應的效能 log
不適用（無高風險改動，無需對應 perf-log）。

## 建議
不需要補跑效能檢查。建議 commit message：

```
docs(plans): update mobile-upload-experience and page-happened-at plans
```

## 模糊地帶（需人工判斷）
無。
```
