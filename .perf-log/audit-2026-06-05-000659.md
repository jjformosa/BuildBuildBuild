```markdown
# 效能審核報告 2026-06-05-000659

## 判斷結果
PASS

## 涉及的高風險改動
無

兩支檔案均不屬於高風險類別：
- `route.ts`：API route，無新增外部 dependency（`crypto` 為 Node 內建，`sign-media` 為專案內部模組）
- `media-uploader.tsx`：Client Component，僅調整解構欄位與 fallback 邏輯，無 layout / 資源載入影響

## 對應的效能 log
找不到對應記錄（但本次改動不需要效能驗證）

## 建議
不需要補跑效能檢查。

建議 commit message：

```
fix(upload): 以 UUID 命名 S3 key，並於 presign 回應補齊 CloudFront signedUrl

[perf-needed: 無]
```

## 模糊地帶（需人工判斷）

**S3 key 命名改為 UUID**
原格式 `image-${index}` 改為 `${randomUUID().slice(0, 8)}-image`，新舊格式並存於同一 S3 prefix。
→ 確認：現有已存入 DB 的舊格式 key 是否仍可正常讀取？若有 CloudFront cache policy 依賴路徑規則，需確認不受影響。

**改存 signedUrl 而非 s3Url**
CloudFront signed URL 有效期有限，若直接存入 DB 作為 `mediaUrls`，屆時 URL 過期將導致圖片失效。
→ 確認：`signImageUrl()` 產生的是短效 token 還是長效 URL？若短效，考慮改為存 `s3Key`，顯示時再簽章。
```
