I need permission to write the audit file. Please approve the write to `.perf-log/audit-2026-07-10-231316.md`, or I can just output the report here instead.

**判斷結果: PASS**

本次改動僅新增 `forlove10grams/lib/models/collection.ts`（Mongoose schema/model），不涉及 page/layout、圖片/字型、第三方依賴或資料獲取邏輯，屬於低風險，不需要效能驗證。

建議 commit message：
```
feat(collection): add Collection mongoose model
```

模糊地帶：若後續 `app/api/collections/` 會用此 model 做大量聚合查詢，屆時再評估。
