**判斷結果：PASS**

這次改動只是 `quick-capture-bar.tsx` 的錯誤處理邏輯調整（新增 `QuickCaptureRequestError`、`res.json()` 加上 `.catch`），fetch 呼叫方式與資料流程都沒變，屬於介面不變、無新增依賴的 refactor，不需要效能驗證。

報告內容（因權限問題尚未寫入檔案，需要你允許寫入 `.perf-log/audit-2026-07-03-082842.md`）：

```markdown
# 效能審核報告 2026-07-03-082842

## 判斷結果
PASS

## 涉及的高風險改動
無

## 對應的效能 log
找不到對應記錄（本次改動不需要效能驗證，故無需比對）

## 建議
本次改動僅為 `quick-capture-bar.tsx` 內部錯誤處理邏輯調整：新增 `QuickCaptureRequestError` 類別、將 `res.json()` 包上 `.catch(() => null)` 以避免非 JSON 回應時拋出未預期例外。fetch 的呼叫方式、endpoint、資料流程皆未變更，屬於「介面不變、無新增外部依賴」的 refactor，不需要補跑效能檢查。

建議 commit message：
```
fix(quick-capture-bar): guard against non-JSON error responses
```

## 模糊地帶（需人工判斷）
無
```

要我重新嘗試寫入該檔案嗎？
