Waiting on your permission to write the report to `.perf-log/audit-2026-07-10-230303.md`.

Summary of the audit result, in case you want it without approving the write:

**PASS** — the three staged files (`route.ts`, `sign-media.ts`, `transcribe.ts`) add an SSRF/cross-tenant media-URL ownership check and a download-size cap for audio transcription. No page/layout, asset-loading, dependency, or data-fetching-shape changes — none of it touches the client bundle or rendering path, so no perf-log is needed.

Suggested commit message:
```
fix(transcribe): guard media URL ownership and cap audio download size

Reason: mediaUrl was attacker-influenceable via PATCH, allowing SSRF or
cross-book access; unbounded audio downloads could exhaust memory before
Whisper's own 25MB limit kicked in.
```
