# 效能審核報告 2026-07-04-082801

## 判斷結果
WARN

## 涉及的高風險改動
- `forlove10grams/app/read/[bookId]/page.tsx` 渲染的 `ReadPageClient` 有實質內容變更：移除內嵌 `LikeButton`，改為渲染新元件 `BookCloseEnding`（原本按讚流程與交出信件流程被重組進同一個結尾動畫元件）。
- `forlove10grams/components/book-close-ending.tsx`（新增，`'use client'`）：在閱讀頁最後一頁用 `IntersectionObserver` 觸發書本闔上動畫，並用 inline style 驅動兩段約 1100ms 的 CSS transform transition，讀完後延遲顯示 `LikeButton` / `HandoverLetter`（整段時間軸約 3 秒）。
- `forlove10grams/public/ending-cover-illustration.png`（新增，32KB）：以 `background-image` 載入（非 `next/image`），左右兩側各載入一次同一張圖（用 `background-size: 200%` 裁切),會在捲動到最後一頁時觸發載入。
- `forlove10grams/components/like-button.tsx`（新增）：從 `read-page-client.tsx` 抽出，邏輯本身未變，純搬移。

## 對應的效能 log
找不到對應記錄（`.perf-log/` 目前沒有非 audit 的手動效能記錄，只有歷史 audit 報告）。

## 建議
- 建議針對 `/read/[bookId]` 頁面補跑一次效能檢查，重點看：
  - 新的動畫序列（`IntersectionObserver` + 多層 `setTimeout`）是否造成主執行緒卡頓或版面偏移（CLS）。
  - 是否值得把 `ending-cover-illustration.png` 改用 `next/image`，或合併左右兩份重複載入的背景圖。
- 建議 commit message：

```
refactor(read-page): extract like-button and add book-close-ending animation

[perf-needed: /read/[bookId]]
```

## 模糊地帶（需人工判斷）
- `book-close-ending.tsx` 新增的 CSS transition/animation（規則中「新增 CSS animation」屬模糊地帶），是否需要正式效能驗證請自行判斷。
- `forlove10grams/docs/samples/*.html` 與 `docs/samples/assets/cover-full.png` 是設計稿範例文件，不在 app 路由或建置產物範圍內，本次未列入高風險，但若未來內容會搬進實際頁面請留意。
